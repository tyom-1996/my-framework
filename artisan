#!/usr/bin/env php
<?php
require __DIR__ . '/vendor/autoload.php';

use Symfony\Component\Console\Application;
use Comands\createController;

$application = new Application();

# add our commands
$application->add(new createController());

$application->run();